# PayClaw repo status (swarm-managed)

Mission control or a recurring task can run `github_repo_status` / sync for the PayClaw repo (once it is in `managed_repos`) and write a short status here: last sync, open PRs, outstanding tasks, health.

This file is a placeholder. Replace with actual status output when the manage flow runs.
